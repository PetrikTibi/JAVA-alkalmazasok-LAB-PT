package com.example.notebook.repository;

import com.example.notebook.model.Oprendszer;
import org.springframework.data.jpa.repository.JpaRepository;

public interface OprendszerRepo extends JpaRepository<Oprendszer, Integer> {
}